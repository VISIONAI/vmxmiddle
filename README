apt-get install libncurses5-dev haskell-platform postgresql
apt-get install libgd2-xpm-dev

#add .cabal/bin to path
export PATH=$PATH:$HOME/.cabal/bin

cabal update

yesod devel

# Requires postgres 

# default database is
# 
# user: middle
# pass: middle
# db  : middle


sudo su - postgres
psql -c "create database middle;"
psql -c "create user middle with password 'middle';"
psql -c "grant all on database middle to middle"
##################





